# MailWhere v0.12.1 — lean repository and portable package

## Changed

- Removed the generated repo-local Codex/OMX catalog and kept only concise
  MailWhere agent boundaries in `AGENTS.md`.
- Removed the redundant three-sample `MailWhere.TestHarness`; the maintained
  regression suite remains the single behavior check.
- Replaced full-tree document and asset copies with a curated portable operator
  guide, current operational docs, and the README logo.
- Preserved the remaining managed-PC and evidence-gated optimization decisions
  in `docs/PRODUCTION_READINESS.md` before removing local OMX history.
- Kept all logo source files in the repository.

## Verification

- `dotnet test MailWhere.sln --configuration Release --no-restore`
- `dotnet build MailWhere.sln --configuration Release --no-restore`
- `scripts/verify-static.sh`
- `scripts/publish-portable.sh`
- portable package content inspection
- `git diff --check`

## Still external

Managed Windows Classic Outlook, WPF behavior, packaged provider calls, and
company-EDR interaction still require `docs/MANAGED_PC_SMOKE_TEST.md`.
